# ── Dependencies ──
node_modules
**/node_modules

# ── Environment Variables (NEVER commit these) ──
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
backend/.env
frontend/.env
**/.env

# ── Build Outputs (specific paths only — do NOT use **/dist as it blocks frontend/src/dist CSS) ──
frontend/build
backend/dist

# ── Logs ──
*.log
npm-debug.log*

# ── OS Files ──
.DS_Store
Thumbs.db
